{
  "name": "embed-canvas",
  "version": "1.0.0",
  "description": "Generate and auto-update image previews for canvas embeds in notes and other canvases.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "release": "npm run build && mkdir -p release && zip -r release/embed-canvas.zip manifest.json main.js styles.css README.md && ls -la release/"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "canvas"
  ],
  "author": "Horatio",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.16.5",
    "esbuild": "^0.24.0",
    "obsidian": "^1.8.4",
    "tslib": "^2.7.0",
    "typescript": "^5.6.2"
  }
}
